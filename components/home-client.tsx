"use client";
import { useEffect, useMemo, useRef, useState } from "react";
import { Check, Plus, Trash2, X } from "lucide-react";
import { SiteShell } from "@/components/site-shell";
import { HomeArchive } from "@/components/home-archive";
import { TurnstileWidget } from "@/components/turnstile-widget";
import {ProductIcon} from "@/components/product-brand";
import {CharacterArt,BlinkSticker} from "@/components/old-web-art";
import { Dialog, DialogContent, DialogDescription, DialogTitle } from "@/components/ui/dialog";
import { money, normalizeHandle, validForumUsername, validHandle, validPrice, validQuantity, type AccountType, type LossOverview, type SellerLossStats } from "@/lib/domain";

const empty:LossOverview={totals:{unresolved_amount:"0.00",report_count:0,visible_sellers:0,reported_purchase_value:"0.00",accounts_reported_purchased:0},leaders:[],recent:[]};
const count=(value:number|null|undefined)=>new Intl.NumberFormat("en-PH").format(value||0);

export function HomeClient({siteKey,ready}:{siteKey:string;ready:boolean}){
  const [overview,setOverview]=useState<LossOverview>(empty),[loading,setLoading]=useState(true),[unavailable,setUnavailable]=useState(false),[open,setOpen]=useState(false);
  const opener=useRef<HTMLElement|null>(null);
  useEffect(()=>{fetch("/api/overview").then(async r=>{if(!r.ok)throw new Error("unavailable");return r.json() as Promise<LossOverview>}).then(data=>{setOverview(data);setUnavailable(false)}).catch(()=>setUnavailable(true)).finally(()=>setLoading(false));},[]);
  useEffect(()=>{
    const handle=(event:Event)=>{event.preventDefault();opener.current=document.activeElement instanceof HTMLElement?document.activeElement:null;setOpen(true)};
    window.addEventListener("purchase-record:open",handle);
    if(new URLSearchParams(window.location.search).get("record")==="1"){
      const timer=window.setTimeout(()=>{setOpen(true);history.replaceState(null,"",window.location.pathname+window.location.hash)},0);
      return()=>{window.clearTimeout(timer);window.removeEventListener("purchase-record:open",handle)};
    }
    return()=>window.removeEventListener("purchase-record:open",handle);
  },[]);
  function changeOpen(next:boolean){setOpen(next);if(!next)requestAnimationFrame(()=>opener.current?.focus())}
  return <SiteShell><main><HomeArchive overview={overview} loading={loading} unavailable={unavailable}/><LossDialog open={open} onOpenChange={changeOpen} siteKey={siteKey} ready={ready}/></main></SiteShell>;
}

type DraftItem={id:string;accountType:string;otherAccountType:string;quantity:string;unitPrice:string};
const newItem=():DraftItem=>({id:crypto.randomUUID(),accountType:"",otherAccountType:"",quantity:"1",unitPrice:""});

function LossDialog({open,onOpenChange,siteKey,ready}:{open:boolean;onOpenChange:(open:boolean)=>void;siteKey:string;ready:boolean}){
  const [handle,setHandle]=useState(""),[buyerUsername,setBuyerUsername]=useState(""),[suggestions,setSuggestions]=useState<SellerLossStats[]>([]),[items,setItems]=useState<DraftItem[]>(()=>[{id:"initial",accountType:"",otherAccountType:"",quantity:"1",unitPrice:""}]),[issue,setIssue]=useState(""),[details,setDetails]=useState(""),[accountTypes,setAccountTypes]=useState<AccountType[]>([]),[error,setError]=useState(""),[invalidField,setInvalidField]=useState<string|null>(null),[success,setSuccess]=useState(false),[saving,setSaving]=useState(false);
  const idempotency=useRef(""),formRef=useRef<HTMLFormElement>(null),errorRef=useRef<HTMLParagraphElement>(null);
  useEffect(()=>{if(!idempotency.current)idempotency.current=crypto.randomUUID()},[]);
  useEffect(()=>{fetch("/api/account-types").then(r=>r.ok?r.json() as Promise<AccountType[]>:[]).then(setAccountTypes).catch(()=>setAccountTypes([]));},[]);
  useEffect(()=>{const normalized=normalizeHandle(handle);const timer=setTimeout(()=>{if(!normalized){setSuggestions([]);return}fetch(`/api/sellers?search=${encodeURIComponent(normalized)}`).then(async r=>r.ok?r.json() as Promise<SellerLossStats[]>:[]).then(setSuggestions).catch(()=>setSuggestions([]))},220);return()=>clearTimeout(timer)},[handle]);
  const totals=useMemo(()=>items.reduce((result,item)=>{const quantity=validQuantity(item.quantity)?Number(item.quantity):0,price=validPrice(item.unitPrice)?Number(item.unitPrice):0;return {accounts:result.accounts+quantity,amount:result.amount+quantity*price}}, {accounts:0,amount:0}),[items]);
  function showError(message:string,fieldId?:string){setError(message);setInvalidField(fieldId??null);requestAnimationFrame(()=>{if(fieldId)document.getElementById(fieldId)?.focus();else errorRef.current?.focus()})}
  function clearFieldError(){if(error){setError("");setInvalidField(null)}}
  function updateItem(id:string,patch:Partial<DraftItem>){setItems(current=>current.map(item=>item.id===id?{...item,...patch}:item));clearFieldError()}
  function reset(){setHandle("");setBuyerUsername("");setSuggestions([]);setItems([newItem()]);setIssue("");setDetails("");setError("");setSuccess(false);idempotency.current=crypto.randomUUID();if(formRef.current){const file=formRef.current.querySelector<HTMLInputElement>("#loss-evidence");if(file)file.value=""}}
  async function submit(event:React.FormEvent<HTMLFormElement>){
    event.preventDefault();setError("");setInvalidField(null);
    if(!validForumUsername(buyerUsername)){showError("Enter your forum username (2 to 64 characters).","loss-buyer");return}
    if(!validHandle(handle)){showError("Enter a valid seller handle.","loss-seller");return}
    if(items.length<1||items.length>8){showError("Add between one and eight products.","loss-product-0");return}
    for(let index=0;index<items.length;index++){const item=items[index];if(!accountTypes.some(type=>type.slug===item.accountType)){showError(`Choose a product for item ${index+1}.`,`loss-product-${index}`);return}if(item.accountType==="other"&&(item.otherAccountType.trim().length<2||item.otherAccountType.trim().length>40)){showError("Describe the other product in 2 to 40 characters.",`loss-other-${index}`);return}if(!validQuantity(item.quantity)){showError("Enter between 1 and 10,000 accounts.",`loss-quantity-${index}`);return}if(!validPrice(item.unitPrice)){showError("Enter a valid price per account in PHP.",`loss-price-${index}`);return}}
    if(!issue){showError("Choose what remains unresolved.","loss-issue");return}if(details.trim().length<10){showError("Add at least 10 characters describing what happened.","loss-details");return}if(!ready){showError("Reporting is unavailable until the data service is configured.");return}
    const form=new FormData(event.currentTarget);form.set("buyerUsername",buyerUsername.trim());form.set("seller",normalizeHandle(handle));form.set("items",JSON.stringify(items.map(({accountType,otherAccountType,quantity,unitPrice})=>({accountType,otherAccountType:accountType==="other"?otherAccountType.trim():null,quantity,unitPrice}))));form.set("issueType",issue);form.set("details",details.trim());form.set("idempotencyKey",idempotency.current);form.set("turnstileToken",String(form.get("cf-turnstile-response")||""));setSaving(true);
    try{const response=await fetch("/api/loss-reports",{method:"POST",headers:{"idempotency-key":idempotency.current},body:form});const result=await response.json() as {error?:string};if(!response.ok)throw new Error(result.error||"We couldn't save the report.");setSuccess(true)}catch(e){showError(e instanceof Error?e.message:"We couldn't save the report.")}finally{setSaving(false)}
  }
  const input="archive-input h-12 w-full px-3 text-sm outline-none";
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="intake-window max-h-[calc(100dvh-1rem)] w-[calc(100%-1rem)] max-w-[1040px] overflow-x-hidden overflow-y-auto border-2 border-foreground bg-card p-0 shadow-[var(--shadow-float)] sm:max-w-[1040px] md:max-h-[calc(100dvh-3rem)]">
    {success?<div className="grid min-h-[520px] place-items-center overflow-hidden p-8 text-center"><div className="relative z-10"><CharacterArt character="jester" pose="sign" className="mx-auto -mb-8 w-48"/><span className="mx-auto grid size-14 place-items-center rounded-full bg-foreground text-background"><Check size={24}/></span><p className="mt-7 gallery-label text-muted-foreground">Submission received</p><DialogTitle className="mt-3 text-5xl font-semibold tracking-[-.07em]">Pending review.</DialogTitle><DialogDescription className="mx-auto mt-4 max-w-sm text-sm leading-6">The complete report enters the public index only if a moderator approves it.</DialogDescription><div className="mt-8 flex flex-wrap justify-center gap-2"><button onClick={()=>onOpenChange(false)} className="min-h-11 border border-foreground px-5 text-sm font-semibold">Close</button><button onClick={reset} className="min-h-11 bg-foreground px-5 text-sm font-semibold text-background">Report another loss</button></div></div></div>:<div className="grid min-w-0 grid-cols-[minmax(0,1fr)] md:grid-cols-[minmax(0,1.35fr)_minmax(260px,.65fr)]"><div className="min-w-0 p-6 md:p-9"><p className="gallery-label border-b-2 border-foreground bg-[var(--lime)] px-3 py-2 text-[#251634]">● ● ● &nbsp; REPORT DESK / PRIVATE INTAKE / 01 <BlinkSticker kind="new"/></p><DialogTitle className="display-type mt-4 text-3xl md:text-4xl">Report an unresolved loss.</DialogTitle><DialogDescription className="mt-3 max-w-md text-sm leading-6">Add every software product from this purchase. The unresolved total is calculated automatically.</DialogDescription>
      <form ref={formRef} onSubmit={submit} className="mt-8 min-w-0 space-y-5" noValidate><div className="grid min-w-0 gap-3 sm:grid-cols-2"><div className="min-w-0"><label htmlFor="loss-buyer" className="mb-2 block text-xs font-semibold">Your forum username</label><input id="loss-buyer" value={buyerUsername} onChange={e=>{setBuyerUsername(e.target.value);clearFieldError()}} autoComplete="off" maxLength={64} placeholder="Your username" aria-invalid={invalidField==="loss-buyer"} className={input}/><p className="mt-1 text-[11px] text-muted-foreground">Only moderators can see this.</p></div><div className="min-w-0"><label htmlFor="loss-seller" className="mb-2 block text-xs font-semibold">Seller’s forum username</label><input id="loss-seller" list="seller-suggestions" value={handle} onChange={e=>{setHandle(e.target.value);clearFieldError()}} autoComplete="off" placeholder="@seller" aria-invalid={invalidField==="loss-seller"} className={input}/><datalist id="seller-suggestions">{suggestions.map(s=><option value={s.normalized_username} key={s.id}/>)}</datalist></div></div>
        <fieldset><legend className="text-xs font-semibold">Software products purchased</legend><div className="mt-2 space-y-3">{items.map((item,index)=>{const selected=accountTypes.find(type=>type.slug===item.accountType);const subtotal=(validQuantity(item.quantity)&&validPrice(item.unitPrice))?Number(item.quantity)*Number(item.unitPrice):0;return <div key={item.id} className="border border-foreground/20 p-3"><div className="mb-3 flex items-center justify-between gap-3"><span className="gallery-label text-muted-foreground">Product {String(index+1).padStart(2,"0")}</span>{items.length>1&&<button type="button" onClick={()=>setItems(current=>current.filter(value=>value.id!==item.id))} className="inline-flex min-h-9 items-center gap-1 border border-foreground/20 px-2 text-[11px] font-semibold" aria-label={`Remove product ${index+1}`}><Trash2 size={13}/> Remove</button>}</div><div className="grid gap-3 sm:grid-cols-2"><div><label htmlFor={`loss-product-${index}`} className="mb-2 block text-xs font-semibold">Account type</label><div className="relative"><select id={`loss-product-${index}`} value={item.accountType} onChange={e=>updateItem(item.id,{accountType:e.target.value,otherAccountType:e.target.value==="other"?item.otherAccountType:""})} className={`${input} pl-12`} aria-invalid={invalidField===`loss-product-${index}`}><option value="">Select a product</option>{accountTypes.map(type=><option key={type.slug} value={type.slug}>{type.label}</option>)}</select>{selected&&<span className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2"><ProductIcon slug={selected.slug} label={selected.label} size={24}/></span>}</div></div><div><label htmlFor={`loss-quantity-${index}`} className="mb-2 block text-xs font-semibold">Accounts bought</label><input id={`loss-quantity-${index}`} type="number" min="1" max="10000" step="1" value={item.quantity} onChange={e=>updateItem(item.id,{quantity:e.target.value})} className={input} aria-invalid={invalidField===`loss-quantity-${index}`}/></div><div><label htmlFor={`loss-price-${index}`} className="mb-2 block text-xs font-semibold">Price per account · PHP</label><input id={`loss-price-${index}`} type="number" min="0.01" max="1000000" step="0.01" inputMode="decimal" value={item.unitPrice} onChange={e=>updateItem(item.id,{unitPrice:e.target.value})} className={input} aria-invalid={invalidField===`loss-price-${index}`}/></div><div className="border border-foreground/15 p-3"><p className="gallery-label text-muted-foreground">Subtotal</p><p className="mt-2 font-mono text-lg font-semibold tabular">{money(subtotal)}</p></div></div>{item.accountType==="other"&&<div className="mt-3"><label htmlFor={`loss-other-${index}`} className="mb-2 block text-xs font-semibold">Other product name</label><input id={`loss-other-${index}`} value={item.otherAccountType} onChange={e=>updateItem(item.id,{otherAccountType:e.target.value})} maxLength={40} placeholder="Product or service" className={input} aria-invalid={invalidField===`loss-other-${index}`}/></div>}</div>})}</div><button type="button" disabled={items.length>=8} onClick={()=>setItems(current=>[...current,newItem()])} className="mt-3 inline-flex min-h-11 items-center gap-2 border border-foreground px-4 text-xs font-semibold disabled:opacity-40"><Plus size={15}/>Add another product</button><p className="mt-2 text-[11px] text-muted-foreground">Up to eight rows. You may select the same product again when its price differs.</p></fieldset>
        <div className="grid grid-cols-2 border border-foreground"><div className="border-r border-foreground p-3"><p className="gallery-label text-muted-foreground">Total accounts</p><p className="mt-2 font-mono text-xl font-semibold tabular">{count(totals.accounts)}</p></div><div className="p-3"><p className="gallery-label text-muted-foreground">Total unresolved</p><p className="mt-2 font-mono text-xl font-semibold tabular">{money(totals.amount)}</p></div></div>
        <div><label htmlFor="loss-issue" className="mb-2 block text-xs font-semibold">What remains unresolved?</label><select id="loss-issue" value={issue} onChange={e=>{setIssue(e.target.value);clearFieldError()}} aria-invalid={invalidField==="loss-issue"} className={input}><option value="">Select an issue</option><option value="not_delivered">Not delivered</option><option value="refund_not_received">Refund not received</option><option value="other_unresolved">Other unresolved issue</option></select></div>
        <div><label htmlFor="loss-details" className="mb-2 block text-xs font-semibold">Brief description · private</label><textarea id="loss-details" value={details} onChange={e=>{setDetails(e.target.value);clearFieldError()}} minLength={10} maxLength={500} rows={3} placeholder="What happened, and what remains unresolved?" aria-invalid={invalidField==="loss-details"} className={`${input} h-auto min-h-24 py-3`}/></div>
        <div><label htmlFor="loss-evidence" className="mb-2 block text-xs font-semibold">Image evidence · optional and private</label><input id="loss-evidence" name="evidence" type="file" accept="image/jpeg,image/png,image/webp" className="block w-full text-xs file:mr-3 file:min-h-10 file:border file:border-foreground/25 file:bg-background file:px-3 file:text-xs file:font-semibold"/><p className="mt-2 text-xs leading-5 text-muted-foreground">JPG, PNG, or WebP · up to 5 MB. Do not upload passwords, identity documents, addresses, phone numbers, or unrelated third-party information.</p></div>
        {siteKey&&<TurnstileWidget siteKey={siteKey} action="loss_report"/>}{!ready&&<p role="status" className="border border-foreground/25 p-3 text-xs leading-5">Submissions open after the data service, private storage, bot protection, and administrator access are configured.</p>}{error&&<p id="loss-error" ref={errorRef} role="alert" tabIndex={-1} className="border-2 border-foreground bg-background p-3 text-sm font-medium leading-5 focus:outline-none">{error}</p>}
        <div className="flex flex-wrap gap-2 pt-2"><button type="submit" disabled={!ready||saving} className="min-h-12 bg-foreground px-5 text-sm font-semibold text-background disabled:cursor-not-allowed disabled:opacity-40">{saving?"Submitting…":"Submit for review"}</button><button type="button" onClick={()=>{reset();onOpenChange(false)}} className="inline-flex min-h-12 items-center gap-2 border border-foreground/25 px-4 text-xs font-semibold"><X size={14}/>Discard</button></div>
      </form></div><aside className="gallery-card noise relative hidden min-h-full min-w-0 flex-col justify-between overflow-visible border-l-2 border-foreground bg-[#3046ad] p-9 text-[#fff8e8] md:flex"><CharacterArt character="clown" pose="pointing" className="absolute -right-16 top-14 w-48"/><div className="gallery-label relative">fraus / PHP</div><div className="relative min-w-0"><p className="font-semibold leading-[.9] tracking-[-.08em] text-[5rem]">@</p><p className="mt-8 gallery-label opacity-55">Accounts reported purchased</p><p className="mt-2 font-mono text-4xl font-medium tabular">{count(totals.accounts)}</p><div className="mt-6 border-t border-background/25 pt-6"><p className="gallery-label opacity-55">Amount still unresolved</p><p className="mt-2 break-all font-mono text-3xl font-medium tabular">{money(totals.amount)}</p></div><p className="mt-8 text-xs leading-5 opacity-60">Product marks identify reported products. fraus is not affiliated with their owners.</p></div></aside></div>}
  </DialogContent></Dialog>;
}
