"use client";
import { useEffect, useMemo, useRef, useState } from "react";
import Link from "next/link";
import Image from "next/image";
import { motion } from "motion/react";
import { ArrowDown, ArrowRight, ArrowUpRight, Check, Crown, X } from "lucide-react";
import { SiteShell } from "@/components/site-shell";
import { SellerSearch } from "@/components/seller-search";
import { RecordTrigger } from "@/components/record-trigger";
import { TurnstileWidget } from "@/components/turnstile-widget";
import {AccountTypeBadges} from "@/components/account-type-badges";
import { Dialog, DialogContent, DialogDescription, DialogTitle } from "@/components/ui/dialog";
import { manilaToday, money, normalizeHandle, validForumUsername, validHandle, validPrice, validPurchaseDate, validQuantity, validUnresolvedAmount, type AccountType, type LossOverview, type SellerLossStats } from "@/lib/domain";

const empty:LossOverview={totals:{unresolved_amount:"0.00",report_count:0,visible_sellers:0,reported_purchase_value:"0.00",accounts_reported_purchased:0},leaders:[],recent:[]};
const count=(value:number|null|undefined)=>new Intl.NumberFormat("en-PH").format(value||0);

export function HomeClient({siteKey,ready}:{siteKey:string;ready:boolean}){
  const [overview,setOverview]=useState<LossOverview>(empty),[loading,setLoading]=useState(true),[unavailable,setUnavailable]=useState(false),[open,setOpen]=useState(false);
  const opener=useRef<HTMLElement|null>(null);
  useEffect(()=>{fetch("/api/overview").then(async r=>{if(!r.ok)throw new Error("unavailable");return r.json() as Promise<LossOverview>}).then(data=>{setOverview(data);setUnavailable(false)}).catch(()=>setUnavailable(true)).finally(()=>setLoading(false));},[]);
  useEffect(()=>{
    const handle=(event:Event)=>{event.preventDefault();opener.current=document.activeElement instanceof HTMLElement?document.activeElement:null;setOpen(true)};
    window.addEventListener("purchase-record:open",handle);
    if(new URLSearchParams(window.location.search).get("record")==="1"){
      const timer=window.setTimeout(()=>{setOpen(true);history.replaceState(null,"",window.location.pathname+window.location.hash)},0);
      return()=>{window.clearTimeout(timer);window.removeEventListener("purchase-record:open",handle)};
    }
    return()=>window.removeEventListener("purchase-record:open",handle);
  },[]);
  function changeOpen(next:boolean){setOpen(next);if(!next)requestAnimationFrame(()=>opener.current?.focus())}
  return <SiteShell><main>
    <section className="relative overflow-hidden border-b border-white/15 bg-[#090909] text-[#f5f1e8]">
      <div className="hall-radial relative mx-auto max-w-[1500px] px-5 pb-14 pt-10 md:px-9 md:pb-20 md:pt-16">
        <div className="flex flex-wrap items-center justify-between gap-4 border-b border-white/20 pb-4 gallery-label text-white/60"><span>Reviewed buyer reports <span className="mx-2">/</span> PHP</span><span>Ranked by amount still unresolved</span></div>
        <div className="mt-9 max-w-6xl"><div className="flex items-center gap-3 text-[#c8a96b]"><Crown size={20}/><span className="gallery-label">The leading three</span></div><h1 className="mt-5 text-balance text-[clamp(3.5rem,10vw,9rem)] font-semibold leading-[.78] tracking-[-.09em]">HALL OF<br/><span className="text-[#c8a96b]">UNRESOLVED</span><br/>PROMISES.</h1><p className="mt-7 max-w-2xl text-sm leading-6 text-white/60">A ranked record of approved, still-open buyer reports. Placement reflects reported unresolved amounts and does not independently establish wrongdoing.</p></div>
        <div className="mt-10 grid grid-cols-2 border border-white/20 md:grid-cols-5">
          <HallTotal label="Reported purchase value" value={money(overview.totals.reported_purchase_value)}/><HallTotal label="Accounts reported purchased" value={count(overview.totals.accounts_reported_purchased)}/><HallTotal label="Amount still unresolved" value={money(overview.totals.unresolved_amount)}/><HallTotal label="Approved open reports" value={count(overview.totals.report_count)}/><HallTotal label="Visible sellers" value={count(overview.totals.visible_sellers)} wide/>
        </div>
        <div className="mt-12 grid min-w-0 gap-4 lg:grid-cols-[minmax(0,1fr)_minmax(0,1.16fr)_minmax(0,1fr)] lg:items-end">
          <HallProfileCard rank={1} seller={overview.leaders[0]} loading={loading}/><HallProfileCard rank={2} seller={overview.leaders[1]} loading={loading}/><HallProfileCard rank={3} seller={overview.leaders[2]} loading={loading}/>
        </div>
        <div className="mt-6 flex flex-wrap items-center justify-between gap-4 text-xs text-white/55"><span role="status">{loading?"Loading the hall…":unavailable?"Live ranking unavailable until the data service is connected.":`${count(overview.totals.visible_sellers)} sellers · ${count(overview.totals.report_count)} reviewed reports`}</span><RecordTrigger label="Report unresolved loss" className="border-white bg-white text-black hover:bg-[#c8a96b]"/></div>
      </div>
    </section>

    <section className="noise relative overflow-hidden border-b border-foreground/20">
      <div className="relative mx-auto max-w-[1380px] px-5 py-12 md:px-9 md:py-20">
        <div className="gallery-label text-muted-foreground">Buyer report index <span className="mx-2">/</span> Ongoing</div>
        <div className="mt-7 grid min-w-0 gap-8 lg:grid-cols-[minmax(0,1fr)_340px] lg:items-end"><div><h2 className="text-[clamp(3rem,9vw,8rem)] font-semibold leading-[.8] tracking-[-.09em]">SEARCH THE<br/>PUBLIC RECORD.</h2></div><div><p className="gallery-label mb-3 text-muted-foreground">Find a seller</p><SellerSearch hero/><a href="#gallery" className="mt-5 inline-flex min-h-11 items-center gap-3 text-xs font-medium uppercase tracking-[.12em] hover:underline">View the full index <ArrowDown size={14}/></a></div></div>
      </div>
    </section>

    <section id="gallery" className="border-t border-foreground/20 bg-card"><div className="mx-auto max-w-[1500px] px-5 py-14 md:px-9 md:py-20">
      <div className="flex flex-wrap items-end justify-between gap-5"><div><p className="gallery-label text-muted-foreground">The full index</p><h2 className="mt-2 text-[clamp(2.55rem,8vw,7rem)] font-semibold leading-[.85] tracking-[-.085em]">LEADERBOARD.</h2></div><span className="gallery-label text-muted-foreground">Open reports / PHP</span></div>
      <div className="mt-10 border-t-2 border-foreground">
        <div className="hidden grid-cols-[42px_52px_minmax(180px,1fr)_150px_105px_150px_95px_20px] gap-4 border-b border-foreground/30 py-3 gallery-label text-muted-foreground md:grid"><span>Rank</span><span/><span>Seller</span><span>Reported value</span><span>Accounts</span><span>Unresolved</span><span>Reports</span><span/></div>
        {loading?<div className="py-14 text-sm text-muted-foreground" role="status">Loading reports…</div>:unavailable?<Unavailable/>:overview.leaders.length?overview.leaders.map((seller,index)=><Link key={seller.id} href={`/seller/${seller.normalized_username}`} aria-label={`View rank ${index+1}, @${seller.username}`} className="group grid min-w-0 grid-cols-[32px_44px_minmax(0,1fr)] gap-3 border-b border-foreground/20 py-5 transition-colors hover:bg-foreground hover:px-4 hover:text-background md:min-h-24 md:grid-cols-[42px_52px_minmax(180px,1fr)_150px_105px_150px_95px_20px] md:items-center md:gap-4"><span className="gallery-label opacity-55">{String(index+1).padStart(2,"0")}</span><SellerAvatar seller={seller} size={52}/><span className="min-w-0"><span className="block truncate text-xl font-semibold tracking-[-.05em] md:text-2xl">@{seller.username}</span><AccountTypeBadges types={seller.account_types||[]} limit={3}/></span><span className="col-span-3 mt-2 grid grid-cols-2 gap-4 border-t border-current/20 pt-3 md:contents"><MobileMetric label="Reported value" value={money(seller.reported_purchase_value)}/><MobileMetric label="Accounts" value={count(seller.accounts_reported_purchased)}/><MobileMetric label="Unresolved" value={money(seller.unresolved_amount)}/><MobileMetric label="Open reports" value={count(seller.report_count)}/></span><ArrowUpRight className="hidden size-4 md:block"/></Link>):<div className="grid min-h-64 place-items-center border-b border-foreground/20 text-center"><div><p className="text-4xl font-semibold tracking-[-.06em]">No reviewed reports yet.</p><p className="mt-3 text-sm text-muted-foreground">Profiles appear after moderator approval.</p></div></div>}
      </div>
      <div className="mt-7 flex flex-wrap items-start justify-between gap-4 text-xs leading-5 text-muted-foreground"><p className="max-w-2xl">Buyer reports are reviewed for inclusion, not independently verified. Reported purchase value is neither verified revenue nor profit, and an unresolved amount is a claim rather than a finding of fraud.</p><Link href="/methodology" className="inline-flex min-h-8 items-center gap-2 font-semibold text-foreground underline underline-offset-4">How rankings work <ArrowRight size={13}/></Link></div>
    </div></section>
    <LossDialog open={open} onOpenChange={changeOpen} siteKey={siteKey} ready={ready}/>
  </main></SiteShell>;
}

function HallTotal({label,value,wide=false}:{label:string;value:string;wide?:boolean}){return <div className={`min-w-0 border-white/20 p-4 md:border-r md:p-5 ${wide?"col-span-2 md:col-span-1":"border-b md:border-b-0"}`}><p className="break-words gallery-label text-white/45">{label}</p><p className="mt-2 break-all font-mono text-xl font-semibold tracking-[-.04em] tabular text-white md:text-2xl">{value}</p></div>}

function HallProfileCard({seller,rank,loading=false}:{seller?:SellerLossStats;rank:number;loading?:boolean}){
  const accent=rank===1?"#c8a96b":rank===2?"#aeb6bf":"#a66f47",primary=rank===1;
  const content=<motion.article initial={{opacity:.65,y:12}} animate={{opacity:1,y:0}} transition={{duration:.4,delay:rank*.07}} className={`gallery-card group flex h-full min-h-[470px] flex-col justify-between border bg-[#111] p-5 text-[#f5f1e8] md:p-7 ${primary?"md:min-h-[620px] lg:min-h-[690px]":"md:min-h-[520px] lg:min-h-[580px]"}`} style={{borderColor:accent}}>
    <div className="relative flex items-center justify-between gap-4"><span className="gallery-label text-white/50">Featured profile</span><span className="grid size-12 place-items-center rounded-full border font-mono text-lg font-semibold" style={{borderColor:accent,color:accent}} aria-label={`Rank ${rank}`}>{String(rank).padStart(2,"0")}</span></div>
    <div className="relative mt-12 min-w-0"><span className="pointer-events-none absolute -left-5 -top-28 select-none text-[15rem] font-semibold leading-none tracking-[-.12em] opacity-[.035]" aria-hidden="true">{rank}</span>{seller?<><div className="mb-7 flex flex-wrap items-end gap-4"><SellerAvatar seller={seller} size={primary?116:88}/><AccountTypeBadges types={seller.account_types||[]} limit={3}/></div><p className={`break-all font-semibold leading-[.86] tracking-[-.075em] ${primary?"text-[clamp(3rem,6vw,6.8rem)]":"text-[clamp(2.6rem,4vw,4.8rem)]"}`}>@{seller.username}</p><div className="mt-8 grid grid-cols-2 border-l border-t border-white/20"><HallMetric label="Reported purchase value" value={money(seller.reported_purchase_value)}/><HallMetric label="Accounts purchased" value={count(seller.accounts_reported_purchased)}/><HallMetric label="Still unresolved" value={money(seller.unresolved_amount)} accent={accent}/><HallMetric label="Approved open reports" value={count(seller.report_count)}/></div><div className="mt-7 flex items-center justify-between border-t border-white/15 pt-5 gallery-label text-white/45"><span>View public record</span><ArrowUpRight className="size-5 transition-transform group-hover:-translate-y-1 group-hover:translate-x-1" style={{color:accent}}/></div></>:<div className="border-t border-white/20 pt-6"><p className="text-4xl font-semibold tracking-[-.06em]">{loading?"Loading…":"Unassigned"}</p><p className="mt-3 gallery-label text-white/45">Awaiting reviewed reports</p></div>}</div>
  </motion.article>;
  return <div className={rank===1?"order-1 lg:order-2":rank===2?"order-2 lg:order-1":"order-3"}>{seller?<Link href={`/seller/${seller.normalized_username}`} aria-label={`View @${seller.username}, ranked ${rank}, ${money(seller.unresolved_amount)} reported unresolved`} className="block h-full focus-visible:outline-offset-4">{content}</Link>:content}</div>;
}

function HallMetric({label,value,accent}:{label:string;value:string;accent?:string}){return <div className="min-w-0 border-b border-r border-white/20 p-3 md:p-4"><p className="break-words gallery-label text-white/40">{label}</p><p className="mt-2 break-all font-mono text-lg font-semibold tabular md:text-xl" style={accent?{color:accent}:undefined}>{value}</p></div>}
function MobileMetric({label,value}:{label:string;value:string}){return <span className="min-w-0"><span className="block gallery-label opacity-50">{label}</span><span className="mt-1 block break-all font-mono text-sm font-semibold tabular">{value}</span></span>}

function SellerAvatar({seller,size}:{seller:SellerLossStats;size:number}){
  return seller.has_avatar?<Image unoptimized src={`/api/sellers/${seller.id}/avatar?v=${encodeURIComponent(seller.avatar_updated_at||"")}`} alt={`Moderator-selected reference image for @${seller.username}`} width={size} height={size} className="shrink-0 border border-current/25 object-cover" style={{width:size,height:size}}/>:<span aria-hidden="true" className="grid shrink-0 place-items-center border border-current/25 font-mono font-semibold uppercase" style={{width:size,height:size}}>{seller.username.slice(0,1)}</span>;
}

function Unavailable(){return <div role="status" className="grid min-h-56 place-items-center border border-foreground/25 p-8 text-center"><div><p className="text-2xl font-semibold">The index is unavailable.</p><p className="mt-2 text-sm text-muted-foreground">Try again after the data service is connected.</p></div></div>}

function LossDialog({open,onOpenChange,siteKey,ready}:{open:boolean;onOpenChange:(open:boolean)=>void;siteKey:string;ready:boolean}){
  const [handle,setHandle]=useState(""),[buyerUsername,setBuyerUsername]=useState(""),[suggestions,setSuggestions]=useState<SellerLossStats[]>([]),[quantity,setQuantity]=useState("1"),[price,setPrice]=useState(""),[date,setDate]=useState(manilaToday),[unresolved,setUnresolved]=useState(""),[issue,setIssue]=useState(""),[details,setDetails]=useState(""),[accountTypes,setAccountTypes]=useState<AccountType[]>([]),[selectedTypes,setSelectedTypes]=useState<string[]>([]),[otherType,setOtherType]=useState(""),[error,setError]=useState(""),[invalidField,setInvalidField]=useState<string|null>(null),[success,setSuccess]=useState(false),[saving,setSaving]=useState(false);
  const idempotency=useRef(""),formRef=useRef<HTMLFormElement>(null),errorRef=useRef<HTMLParagraphElement>(null);
  useEffect(()=>{if(!idempotency.current)idempotency.current=crypto.randomUUID()},[]);
  useEffect(()=>{fetch("/api/account-types").then(r=>r.ok?r.json() as Promise<AccountType[]>:[]).then(setAccountTypes).catch(()=>setAccountTypes([]));},[]);
  useEffect(()=>{const normalized=normalizeHandle(handle);const timer=setTimeout(()=>{if(!normalized){setSuggestions([]);return}fetch(`/api/sellers?search=${encodeURIComponent(normalized)}`).then(async r=>r.ok?r.json() as Promise<SellerLossStats[]>:[]).then(setSuggestions).catch(()=>setSuggestions([]))},220);return()=>clearTimeout(timer)},[handle]);
  const total=useMemo(()=>{const value=Number(quantity)*Number(price);return Number.isFinite(value)&&value>0?money(value):"₱0"},[quantity,price]);
  const validAmount=validUnresolvedAmount(unresolved,quantity,price);
  function showError(message:string,fieldId?:string){setError(message);setInvalidField(fieldId??null);requestAnimationFrame(()=>{if(fieldId)document.getElementById(fieldId)?.focus();else errorRef.current?.focus()})}
  function clearFieldError(){if(error){setError("");setInvalidField(null)}}
  function reset(){setHandle("");setBuyerUsername("");setSuggestions([]);setQuantity("1");setPrice("");setDate(manilaToday());setUnresolved("");setIssue("");setDetails("");setSelectedTypes([]);setOtherType("");setError("");setSuccess(false);idempotency.current=crypto.randomUUID();if(formRef.current){const file=formRef.current.querySelector<HTMLInputElement>("#loss-evidence");if(file)file.value=""}}
  async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setError("");setInvalidField(null);if(!validForumUsername(buyerUsername)){showError("Enter your forum username (2 to 64 characters).","loss-buyer");return}if(!validHandle(handle)){showError("Enter a valid seller handle.","loss-seller");return}if(selectedTypes.length<1||selectedTypes.length>8){showError("Choose between one and eight account types.","loss-account-types");return}if(selectedTypes.includes("other")&&(otherType.trim().length<2||otherType.trim().length>40)){showError("Describe the other account type in 2 to 40 characters.","loss-other-type");return}if(!validPurchaseDate(date)){showError("Choose a valid purchase date.","loss-date");return}if(!validQuantity(quantity)){showError("Enter between 1 and 10,000 accounts.","loss-quantity");return}if(!validPrice(price)){showError("Enter a valid price per account in PHP.","loss-price");return}if(!validAmount){showError("Enter an unresolved amount up to the purchase total.","loss-unresolved");return}if(!issue){showError("Choose what remains unresolved.","loss-issue");return}if(details.trim().length<10){showError("Add at least 10 characters describing what happened.","loss-details");return}if(!ready){showError("Reporting is unavailable until the data service is configured.");return}const form=new FormData(event.currentTarget);form.set("buyerUsername",buyerUsername.trim());form.set("seller",normalizeHandle(handle));form.set("accountTypes",JSON.stringify(selectedTypes));form.set("otherAccountType",otherType.trim());form.set("purchaseDate",date);form.set("quantity",quantity);form.set("unitPrice",price);form.set("unresolvedAmount",unresolved);form.set("issueType",issue);form.set("details",details.trim());form.set("idempotencyKey",idempotency.current);form.set("turnstileToken",String(form.get("cf-turnstile-response")||""));setSaving(true);try{const response=await fetch("/api/loss-reports",{method:"POST",headers:{"idempotency-key":idempotency.current},body:form});const result=await response.json() as {error?:string};if(!response.ok)throw new Error(result.error||"We couldn't save the report.");setSuccess(true)}catch(e){showError(e instanceof Error?e.message:"We couldn't save the report.")}finally{setSaving(false)}}
  const input="h-12 w-full rounded-sm border border-foreground/25 bg-card px-3 text-sm outline-none focus:border-foreground";
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[calc(100dvh-1rem)] w-[calc(100%-1rem)] max-w-[960px] overflow-x-hidden overflow-y-auto rounded-sm border border-foreground bg-card p-0 shadow-[var(--shadow-float)] sm:max-w-[960px] md:max-h-[calc(100dvh-3rem)]">
    {success?<div className="grid min-h-[520px] place-items-center p-8 text-center"><div><span className="mx-auto grid size-14 place-items-center rounded-full bg-foreground text-background"><Check size={24}/></span><p className="mt-7 gallery-label text-muted-foreground">Submission received</p><DialogTitle className="mt-3 text-5xl font-semibold tracking-[-.07em]">Pending review.</DialogTitle><DialogDescription className="mx-auto mt-4 max-w-sm text-sm leading-6">The report enters the public index only if a moderator approves it.</DialogDescription><div className="mt-8 flex flex-wrap justify-center gap-2"><button onClick={()=>onOpenChange(false)} className="min-h-11 border border-foreground px-5 text-sm font-semibold">Close</button><button onClick={reset} className="min-h-11 bg-foreground px-5 text-sm font-semibold text-background">Report another loss</button></div></div></div>:<div className="grid min-w-0 grid-cols-[minmax(0,1fr)] md:grid-cols-[minmax(0,1.15fr)_minmax(0,.85fr)]"><div className="min-w-0 p-6 md:p-9"><p className="gallery-label text-muted-foreground">Private submission / 01</p><DialogTitle className="mt-4 text-4xl font-semibold leading-[.95] tracking-[-.065em] md:text-5xl">Report an unresolved loss.</DialogTitle><DialogDescription className="mt-3 max-w-md text-sm leading-6">Reports are reviewed before they appear in the index.</DialogDescription>
      <form ref={formRef} onSubmit={submit} className="mt-8 min-w-0 space-y-5" noValidate><div className="grid min-w-0 gap-3 sm:grid-cols-2"><div className="min-w-0"><label htmlFor="loss-buyer" className="mb-2 block text-xs font-semibold">Your forum username</label><input id="loss-buyer" value={buyerUsername} onChange={e=>{setBuyerUsername(e.target.value);clearFieldError()}} autoComplete="off" maxLength={64} placeholder="Your username" aria-invalid={invalidField==="loss-buyer"} aria-describedby={invalidField==="loss-buyer"?"buyer-privacy loss-error":"buyer-privacy"} className={input}/><p id="buyer-privacy" className="mt-1 text-[11px] text-muted-foreground">Only moderators can see this.</p></div><div className="min-w-0"><label htmlFor="loss-seller" className="mb-2 block text-xs font-semibold">Seller’s forum username</label><input id="loss-seller" list="seller-suggestions" value={handle} onChange={e=>{setHandle(e.target.value);clearFieldError()}} autoComplete="off" placeholder="@seller" aria-invalid={invalidField==="loss-seller"} aria-describedby={invalidField==="loss-seller"?"loss-error":undefined} className={input}/><datalist id="seller-suggestions">{suggestions.map(s=><option value={s.normalized_username} key={s.id}/>)}</datalist></div></div>
        <fieldset id="loss-account-types"><legend className="mb-2 text-xs font-semibold">Account types purchased</legend><div className="grid grid-cols-2 gap-2 sm:grid-cols-3">{accountTypes.map(type=><label key={type.slug} className="flex min-h-11 items-center gap-2 border border-foreground/25 px-3 text-xs"><input type="checkbox" checked={selectedTypes.includes(type.slug)} onChange={e=>{setSelectedTypes(current=>e.target.checked?[...current,type.slug]:current.filter(v=>v!==type.slug));clearFieldError()}}/>{type.label}</label>)}</div>{selectedTypes.includes("other")&&<input id="loss-other-type" value={otherType} onChange={e=>{setOtherType(e.target.value);clearFieldError()}} maxLength={40} placeholder="Other account type" className={`${input} mt-2`} aria-invalid={invalidField==="loss-other-type"}/>}<p className="mt-2 text-[11px] text-muted-foreground">Choose up to eight. Custom labels are reviewed before publication.</p></fieldset>
        <div className="grid min-w-0 grid-cols-1 gap-3 min-[460px]:grid-cols-2"><div className="min-w-0"><label htmlFor="loss-date" className="mb-2 block text-xs font-semibold">Purchase date</label><input id="loss-date" type="date" value={date} max={manilaToday()} min="2000-01-01" onChange={e=>{setDate(e.target.value);clearFieldError()}} aria-invalid={invalidField==="loss-date"} aria-describedby={invalidField==="loss-date"?"loss-error":undefined} className={input}/></div><div className="min-w-0"><label htmlFor="loss-quantity" className="mb-2 block text-xs font-semibold">Accounts bought</label><input id="loss-quantity" type="number" min="1" max="10000" step="1" value={quantity} onChange={e=>{setQuantity(e.target.value);clearFieldError()}} aria-invalid={invalidField==="loss-quantity"} aria-describedby={invalidField==="loss-quantity"?"loss-error":undefined} className={input}/></div></div>
        <div className="grid min-w-0 grid-cols-1 gap-3 min-[460px]:grid-cols-2"><div className="min-w-0"><label htmlFor="loss-price" className="mb-2 block text-xs font-semibold">Price per account · PHP</label><input id="loss-price" type="number" min="0.01" max="1000000" step="0.01" inputMode="decimal" value={price} onChange={e=>{setPrice(e.target.value);clearFieldError()}} aria-invalid={invalidField==="loss-price"} aria-describedby={invalidField==="loss-price"?"loss-error":undefined} className={input}/></div><div className="min-w-0"><label htmlFor="loss-unresolved" className="mb-2 block text-xs font-semibold">Still unresolved · PHP</label><input id="loss-unresolved" type="number" min="0.01" step="0.01" inputMode="decimal" value={unresolved} onChange={e=>{setUnresolved(e.target.value);clearFieldError()}} aria-invalid={invalidField==="loss-unresolved"} aria-describedby={invalidField==="loss-unresolved"?"amount-hint loss-error":"amount-hint"} className={input}/></div></div><p id="amount-hint" className="text-xs text-muted-foreground">Up to the purchase total: {total}</p>
        <div><label htmlFor="loss-issue" className="mb-2 block text-xs font-semibold">What remains unresolved?</label><select id="loss-issue" value={issue} onChange={e=>{setIssue(e.target.value);clearFieldError()}} aria-invalid={invalidField==="loss-issue"} aria-describedby={invalidField==="loss-issue"?"loss-error":undefined} className={input}><option value="">Select an issue</option><option value="not_delivered">Not delivered</option><option value="refund_not_received">Refund not received</option><option value="other_unresolved">Other unresolved issue</option></select></div>
        <div><label htmlFor="loss-details" className="mb-2 block text-xs font-semibold">Brief description · private</label><textarea id="loss-details" value={details} onChange={e=>{setDetails(e.target.value);clearFieldError()}} minLength={10} maxLength={500} rows={3} placeholder="What happened, and what remains unresolved?" aria-invalid={invalidField==="loss-details"} aria-describedby={invalidField==="loss-details"?"loss-error":undefined} className={`${input} h-auto min-h-24 py-3`}/></div>
        <div><label htmlFor="loss-evidence" className="mb-2 block text-xs font-semibold">Image evidence · optional and private</label><input id="loss-evidence" name="evidence" type="file" accept="image/jpeg,image/png,image/webp" className="block w-full text-xs file:mr-3 file:min-h-10 file:border file:border-foreground/25 file:bg-background file:px-3 file:text-xs file:font-semibold"/><p className="mt-2 text-xs leading-5 text-muted-foreground">JPG, PNG, or WebP · up to 5 MB. Do not upload passwords, identity documents, addresses, phone numbers, or unrelated third-party information.</p></div>
        {siteKey&&<TurnstileWidget siteKey={siteKey} action="loss_report"/>}
        {!ready&&<p role="status" className="border border-foreground/25 p-3 text-xs leading-5">Submissions open after the data service, private storage, bot protection, and administrator access are configured.</p>}{error&&<p id="loss-error" ref={errorRef} role="alert" tabIndex={-1} className="border-2 border-foreground bg-background p-3 text-sm font-medium leading-5 focus:outline-none">{error}</p>}
        <div className="flex flex-wrap gap-2 pt-2"><button type="submit" disabled={!ready||saving} className="min-h-12 bg-foreground px-5 text-sm font-semibold text-background disabled:cursor-not-allowed disabled:opacity-40">{saving?"Submitting…":"Submit for review"}</button><button type="button" onClick={()=>{reset();onOpenChange(false)}} className="inline-flex min-h-12 items-center gap-2 border border-foreground/25 px-4 text-xs font-semibold"><X size={14}/>Discard</button></div>
      </form></div><aside className="gallery-card noise relative hidden min-h-full min-w-0 flex-col justify-between bg-foreground p-9 text-background md:flex"><div className="gallery-label relative">fraus / PHP</div><div className="relative min-w-0"><p className="font-semibold leading-[.9] tracking-[-.08em] text-[5rem]">@</p><p className="mt-8 gallery-label opacity-55">Calculated purchase total</p><p className="mt-2 break-all font-mono text-4xl font-medium tabular">{total}</p><div className="mt-6 border-t border-background/25 pt-6"><p className="gallery-label opacity-55">Amount still unresolved</p><p className="mt-2 break-all font-mono text-3xl font-medium tabular">{validAmount?money(unresolved):"—"}</p></div></div></aside></div>}
  </DialogContent></Dialog>;
}
