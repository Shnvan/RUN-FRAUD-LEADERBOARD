"use client";
import { useEffect, useMemo, useRef, useState } from "react";
import Link from "next/link";
import Script from "next/script";
import { AnimatePresence, motion } from "motion/react";
import { ArrowRight, ArrowUpRight, Check, Database, ReceiptText, ShieldCheck, Sparkles, Trophy } from "lucide-react";
import { SiteShell } from "@/components/site-shell";
import { SellerSearch } from "@/components/seller-search";
import { RecentRecords } from "@/components/public-ui";
import { Combobox, ComboboxInput, ComboboxContent, ComboboxList, ComboboxItem, ComboboxEmpty } from "@/components/ui/combobox";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { manilaToday, money, normalizeHandle, validHandle, type HomeOverview, type SellerStats, type PlatformStats } from "@/lib/domain";

const emptyTotals: PlatformStats={reported_sales:"0.00",accounts_sold:0,recorded_purchases:0,visible_sellers:0};
const emptyOverview: HomeOverview={totals:emptyTotals,leaders:{sales:[],accounts:[],records:[]},recent:[]};
type LeaderKey="sales"|"accounts"|"records";
const leaderMeta:Record<LeaderKey,{label:string;title:string;value:(seller:SellerStats)=>string}>={
  sales:{label:"Reported sales",title:"Highest reported sales",value:s=>money(s.reported_sales)},
  accounts:{label:"Accounts",title:"Most accounts reported",value:s=>new Intl.NumberFormat("en-PH").format(s.accounts_sold)},
  records:{label:"Records",title:"Most approved records",value:s=>new Intl.NumberFormat("en-PH").format(s.recorded_purchases)}
};

export function HomeClient({siteKey,ready}:{siteKey:string;ready:boolean}) {
  const [overview,setOverview]=useState<HomeOverview>(emptyOverview),[unavailable,setUnavailable]=useState(false),[modalOpen,setModalOpen]=useState(false),[leaderTab,setLeaderTab]=useState<LeaderKey>("sales");
  const modalOpener=useRef<HTMLElement|null>(null);
  useEffect(()=>{fetch("/api/overview").then(async r=>{if(!r.ok)throw new Error("unavailable");return r.json() as Promise<HomeOverview>}).then(data=>{setOverview(data);setUnavailable(false)}).catch(()=>setUnavailable(true));},[]);
  useEffect(()=>{const open=(event:Event)=>{event.preventDefault();modalOpener.current=document.activeElement instanceof HTMLElement?document.activeElement:null;setModalOpen(true)};window.addEventListener("purchase-record:open",open);const timer=window.setTimeout(()=>{if(new URLSearchParams(window.location.search).get("record")==="1"){setModalOpen(true);history.replaceState(null,"",window.location.pathname+window.location.hash)}},0);return()=>{window.clearTimeout(timer);window.removeEventListener("purchase-record:open",open)};},[]);
  function changeModal(open:boolean){setModalOpen(open);if(!open)requestAnimationFrame(()=>modalOpener.current?.focus())}
  const activeLeaders=overview.leaders[leaderTab];
  return <SiteShell><main>
    <section className="page-grid noise relative overflow-hidden border-b border-foreground/8">
      <div className="relative mx-auto grid min-h-[650px] max-w-[1240px] items-end gap-12 px-5 pb-16 pt-20 md:px-8 md:pb-20 lg:grid-cols-12 lg:pt-28">
        <motion.div initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} transition={{duration:.55}} className="min-w-0 lg:col-span-8">
          <div className="mb-8 inline-flex items-center gap-2 rounded-full border border-foreground/10 bg-card/70 px-3 py-1.5 font-mono text-[10px] uppercase tracking-[.14em]"><span className="signal-dot size-1.5 rounded-full bg-accent"/>Buyer-reported. Moderator reviewed.</div>
          <h1 className="max-w-[880px] text-balance text-[clamp(4rem,10vw,8.6rem)] font-semibold leading-[.82] tracking-[-.085em]">See what buyers paid.</h1>
          <p className="mt-9 max-w-xl text-base leading-7 text-muted-foreground md:text-lg">A public record of approved buyer submissions, preserving the price and quantity reported for every purchase.</p>
          <div className="mt-9 max-w-xl"><SellerSearch hero/></div>
        </motion.div>
        <motion.aside initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} transition={{duration:.55,delay:.12}} className="lg:col-span-4 lg:pb-2">
          <div className="rounded-[28px] bg-foreground p-6 text-background md:p-7"><div className="flex items-center justify-between"><p className="font-mono text-[10px] uppercase tracking-[.16em] text-background/55">Public methodology</p><Sparkles size={16} className="text-accent"/></div><p className="mt-16 text-2xl font-medium leading-tight tracking-[-.04em]">Totals move only when a moderator approves a submitted purchase.</p><Link href="/methodology" className="mt-8 inline-flex items-center gap-2 text-xs font-semibold text-accent">How the record works <ArrowRight size={13}/></Link></div>
        </motion.aside>
      </div>
    </section>

    <section className="mx-auto max-w-[1240px] px-5 py-20 md:px-8 md:py-28">
      <div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="font-mono text-[10px] uppercase tracking-[.16em] text-muted-foreground">Approved activity</p><h2 className="mt-2 text-4xl font-semibold tracking-[-.065em] md:text-5xl">The record at a glance.</h2></div><p className="max-w-md text-sm leading-6 text-muted-foreground">Buyer-Reported Sales are exposure recorded here. They are neither complete revenue nor proof of wrongdoing.</p></div>
      <div className="grid gap-3 md:grid-cols-2 lg:grid-cols-4">
        <StatCard label="Buyer-Reported Sales" value={money(overview.totals.reported_sales)} featured/>
        <StatCard label="Accounts reported" value={String(overview.totals.accounts_sold)}/>
        <StatCard label="Approved purchases" value={String(overview.totals.recorded_purchases)}/>
        <StatCard label="Visible sellers" value={String(overview.totals.visible_sellers)}/>
      </div>
      {unavailable&&<p role="status" className="mt-4 rounded-xl border border-foreground/10 bg-card px-4 py-3 text-sm text-muted-foreground">Live totals are unavailable while the data service is being configured.</p>}
    </section>

    <section id="leaderboard" className="bg-foreground py-20 text-background md:py-28">
      <div className="mx-auto max-w-[1240px] px-5 md:px-8">
        <div className="grid gap-8 lg:grid-cols-12 lg:items-end"><div className="lg:col-span-8"><p className="font-mono text-[10px] uppercase tracking-[.16em] text-background/45">Reported exposure</p><h2 className="mt-3 text-balance text-4xl font-semibold tracking-[-.065em] md:text-6xl">The sellers buyers report most.</h2></div><p className="text-sm leading-6 text-background/55 lg:col-span-4">Ranked from approved buyer submissions. Rankings describe reported purchasing activity and make no claim of fraud, profit, or complete revenue.</p></div>
        <div className="mt-12 grid gap-3 lg:grid-cols-3">{(["sales","accounts","records"] as LeaderKey[]).map((key,index)=><LeaderCard key={key} seller={overview.leaders[key][0]} kind={key} index={index}/>)}</div>
        <div className="mt-12 overflow-hidden rounded-[28px] border border-background/12 bg-background/[.045]">
          <div className="flex flex-col justify-between gap-5 border-b border-background/10 p-5 md:flex-row md:items-center md:px-7"><div><p className="text-sm font-semibold">Full leaderboard</p><p className="mt-1 text-xs text-background/45">Top ten approved seller records</p></div><Tabs value={leaderTab} onValueChange={v=>setLeaderTab(v as LeaderKey)}><TabsList className="bg-background/8 text-background/55"><TabsTrigger value="sales" className="text-background/55 data-[state=active]:bg-accent data-[state=active]:text-accent-foreground">Sales</TabsTrigger><TabsTrigger value="accounts" className="text-background/55 data-[state=active]:bg-accent data-[state=active]:text-accent-foreground">Accounts</TabsTrigger><TabsTrigger value="records" className="text-background/55 data-[state=active]:bg-accent data-[state=active]:text-accent-foreground">Records</TabsTrigger></TabsList></Tabs></div>
          <AnimatePresence mode="wait"><motion.div key={leaderTab} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-8}} transition={{duration:.2}}>{activeLeaders.length?<div>{activeLeaders.map((seller,index)=><LeaderboardRow key={seller.id} seller={seller} index={index} kind={leaderTab}/>)}</div>:<div className="grid min-h-52 place-items-center p-8 text-center"><div><Trophy size={22} className="mx-auto text-background/35"/><p className="mt-4 text-sm font-medium">No rankings yet</p><p className="mt-1 text-xs text-background/45">Approved purchases will establish the leaderboard.</p></div></div>}</motion.div></AnimatePresence>
        </div>
      </div>
    </section>

    <div className="mx-auto max-w-[1240px] px-5 md:px-8"><RecentRecords rows={overview.recent}/><section className="mt-20 grid gap-3 md:grid-cols-3"><TrustCard icon={<Database/>} number="01" title="Original prices preserved" text="Every approved record keeps the unit price reported for its purchase date."/><TrustCard icon={<ShieldCheck/>} number="02" title="Private evidence" text="Evidence and buyer details remain visible only to authorized moderators."/><TrustCard icon={<ReceiptText/>} number="03" title="Corrections routed" text="Every public purchase can be flagged through a private structured request."/></section></div>
    <PurchaseDialog open={modalOpen} onOpenChange={changeModal} siteKey={siteKey} ready={ready}/>
  </main></SiteShell>;
}

function StatCard({label,value,featured=false}:{label:string;value:string;featured?:boolean}) {return <motion.article initial={{y:10}} whileInView={{y:0}} viewport={{once:true,amount:.25}} className={`min-h-48 rounded-[24px] p-5 md:p-6 ${featured?"bg-accent text-accent-foreground":"premium-card"}`}><p className="font-mono text-[10px] uppercase tracking-[.14em] opacity-60">{label}</p><motion.p key={value} initial={{opacity:.7,y:5}} animate={{opacity:1,y:0}} className="mt-16 text-4xl font-semibold tracking-[-.065em] tabular">{value}</motion.p></motion.article>}

function LeaderCard({seller,kind,index}:{seller?:SellerStats;kind:LeaderKey;index:number}) {return <motion.article initial={{y:12}} whileInView={{y:0}} viewport={{once:true}} transition={{delay:index*.06}} className={`min-h-64 rounded-[24px] border p-6 ${index===0?"border-accent bg-accent text-accent-foreground":"border-background/12 bg-background/[.045]"}`}><div className="flex justify-between font-mono text-[10px] uppercase tracking-[.14em] opacity-55"><span>0{index+1}</span><span>{leaderMeta[kind].label}</span></div>{seller?<><Link href={`/seller/${seller.normalized_username}`} className="mt-20 flex items-center justify-between text-xl font-semibold tracking-[-.04em] hover:underline"><span>@{seller.username}</span><ArrowUpRight size={17}/></Link><p className="mt-2 text-3xl font-semibold tracking-[-.055em] tabular">{leaderMeta[kind].value(seller)}</p></>:<div className="mt-20"><p className="text-lg font-semibold">Awaiting approved data</p><p className="mt-2 text-sm opacity-55">{leaderMeta[kind].title}</p></div>}</motion.article>}

function LeaderboardRow({seller,index,kind}:{seller:SellerStats;index:number;kind:LeaderKey}) {return <Link href={`/seller/${seller.normalized_username}`} className="grid grid-cols-[36px_1fr_auto] items-center gap-3 border-b border-background/8 px-5 py-4 last:border-0 transition hover:bg-background/[.045] md:grid-cols-[56px_1fr_150px_150px_20px] md:px-7"><span className="font-mono text-xs text-background/35">{String(index+1).padStart(2,"0")}</span><span className="font-semibold">@{seller.username}</span><span className="font-mono text-sm font-semibold tabular">{leaderMeta[kind].value(seller)}</span><span className="hidden font-mono text-xs text-background/45 md:block">{seller.accounts_sold} accounts · {seller.recorded_purchases} records</span><ArrowUpRight size={13} className="hidden text-background/45 md:block"/></Link>}

function TrustCard({icon,number,title,text}:{icon:React.ReactNode;number:string;title:string;text:string}) {return <article className="rounded-[24px] border border-foreground/10 p-6"><div className="flex items-center justify-between text-muted-foreground"><span className="[&_svg]:size-18 [&_svg]:h-5 [&_svg]:w-5">{icon}</span><span className="font-mono text-[10px]">{number}</span></div><h3 className="mt-16 text-lg font-semibold tracking-[-.035em]">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{text}</p></article>}

function PurchaseDialog({open,onOpenChange,siteKey,ready}:{open:boolean;onOpenChange:(open:boolean)=>void;siteKey:string;ready:boolean}) {
  const [handle,setHandle]=useState(""),[selected,setSelected]=useState<string|null>(null),[suggestions,setSuggestions]=useState<SellerStats[]>([]),[quantity,setQuantity]=useState("1"),[price,setPrice]=useState(""),[date,setDate]=useState(manilaToday),[status,setStatus]=useState(""),[error,setError]=useState(""),[saving,setSaving]=useState(false),[successHandle,setSuccessHandle]=useState("");
  const idempotency=useRef(""); const formRef=useRef<HTMLFormElement>(null);
  useEffect(()=>{if(!idempotency.current)idempotency.current=crypto.randomUUID()},[]);
  useEffect(()=>{const h=normalizeHandle(handle);const timer=setTimeout(()=>{if(!h){setSuggestions([]);return}fetch(`/api/sellers?search=${encodeURIComponent(h)}`).then(async r=>r.ok?r.json() as Promise<SellerStats[]>:[]).then(setSuggestions).catch(()=>setSuggestions([]))},220);return()=>clearTimeout(timer)},[handle]);
  const total=useMemo(()=>{const q=Number(quantity),p=Number(price);return Number.isFinite(q*p)&&q>0&&p>0?money(q*p):"₱0"},[quantity,price]);
  const match=suggestions.some(s=>s.normalized_username===normalizeHandle(handle));
  function reset(){setHandle("");setSelected(null);setSuggestions([]);setQuantity("1");setPrice("");setDate(manilaToday());setStatus("");setError("");setSuccessHandle("");idempotency.current=crypto.randomUUID();if(formRef.current){const file=formRef.current.querySelector<HTMLInputElement>("#evidence");if(file)file.value=""}}
  async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setError("");setStatus("");if(!validHandle(handle)){setError("Enter a valid seller handle.");return}if(!ready){setError("Purchase recording is unavailable until the service is configured.");return}const form=new FormData(event.currentTarget);const normalized=normalizeHandle(handle);form.set("seller",normalized);form.set("purchaseDate",date);form.set("quantity",quantity);form.set("unitPrice",price);form.set("idempotencyKey",idempotency.current);form.set("turnstileToken",String(form.get("cf-turnstile-response")||""));setSaving(true);try{const response=await fetch("/api/purchases",{method:"POST",body:form});const result=await response.json() as {error?:string};if(!response.ok)throw new Error(result.error||"We couldn't save this purchase.");setSuccessHandle(normalized);setStatus("Purchase recorded. Pending review.");setQuantity("1");setPrice("");setDate(manilaToday());idempotency.current=crypto.randomUUID();}catch(e){setError(e instanceof Error?e.message:"We couldn't save this purchase.")}finally{setSaving(false)}}
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[calc(100dvh-1rem)] w-[calc(100%-1rem)] max-w-[920px] overflow-y-auto rounded-[28px] border-foreground/12 bg-card p-0 shadow-[var(--shadow-float)] sm:max-w-[920px] md:max-h-[calc(100dvh-3rem)]" onEscapeKeyDown={()=>{}}>
    {status?<div className="grid min-h-[560px] place-items-center p-8 text-center"><div><div className="mx-auto grid size-16 place-items-center rounded-full bg-accent text-accent-foreground"><Check size={25}/></div><p className="mt-7 font-mono text-[10px] uppercase tracking-[.16em] text-muted-foreground">Submission received</p><DialogTitle className="mt-3 text-4xl font-semibold tracking-[-.06em]">Pending review.</DialogTitle><DialogDescription className="mx-auto mt-3 max-w-sm leading-6">Public totals update only if a moderator approves this purchase.</DialogDescription><div className="mt-8 flex flex-col justify-center gap-2 sm:flex-row">{match&&<Link href={`/seller/${successHandle}`} onClick={()=>onOpenChange(false)} className="inline-flex h-11 items-center justify-center rounded-full border border-foreground/15 px-5 text-sm font-semibold">View seller record</Link>}<button onClick={()=>{reset()}} className="h-11 rounded-full bg-foreground px-5 text-sm font-semibold text-background">Record another purchase</button></div></div></div>:<div className="grid md:grid-cols-[1.15fr_.85fr]">
      <div className="p-6 md:p-9"><DialogHeader><p className="font-mono text-[10px] uppercase tracking-[.16em] text-muted-foreground">Buyer submission</p><DialogTitle className="mt-2 text-3xl font-semibold tracking-[-.055em]">Record a purchase</DialogTitle><DialogDescription className="leading-6">Add what you paid. A moderator reviews it before anything becomes public.</DialogDescription></DialogHeader>
      <form ref={formRef} onSubmit={submit} className="mt-8 space-y-5" noValidate><div><label className="mb-2 block text-xs font-semibold" htmlFor="seller">Seller handle</label><Combobox items={[...suggestions.map(s=>s.normalized_username),...(!match&&validHandle(handle)?[normalizeHandle(handle)]:[])]} value={selected} onValueChange={value=>{const next=value||"";setSelected(next||null);setHandle(next)}} onInputValueChange={value=>{setHandle(value);if(normalizeHandle(value)!==selected)setSelected(null)}}><ComboboxInput id="seller" showTrigger={false} placeholder="Search or enter a handle" className="w-full [&_[data-slot=input-group-input]]:h-12"/><ComboboxContent><ComboboxList>{suggestions.map(s=><ComboboxItem key={s.id} value={s.normalized_username}>@{s.username}</ComboboxItem>)}{!match&&validHandle(handle)&&<ComboboxItem value={normalizeHandle(handle)}>Add @{normalizeHandle(handle)}</ComboboxItem>}<ComboboxEmpty>No matching seller.</ComboboxEmpty></ComboboxList></ComboboxContent></Combobox></div>
      <div><label className="mb-2 block text-xs font-semibold" htmlFor="date">Purchase date</label><input id="date" type="date" value={date} max={manilaToday()} onChange={e=>setDate(e.target.value)} className="h-12 w-full rounded-xl border border-input bg-card px-3.5 text-sm" required/></div>
      <div className="grid grid-cols-2 gap-3"><div><label className="mb-2 block text-xs font-semibold" htmlFor="quantity">Accounts</label><input id="quantity" type="number" min="1" max="10000" step="1" value={quantity} onChange={e=>setQuantity(e.target.value)} className="h-12 w-full rounded-xl border border-input bg-card px-3.5 text-sm" required/></div><div><label className="mb-2 block text-xs font-semibold" htmlFor="price">Price each</label><div className="flex h-12 items-center rounded-xl border border-input bg-card"><span className="pl-3.5 text-sm text-muted-foreground">₱</span><input id="price" inputMode="decimal" pattern="[0-9]*[.]?[0-9]{0,2}" value={price} onChange={e=>setPrice(e.target.value)} placeholder="0.00" className="min-w-0 flex-1 bg-transparent px-2 text-sm outline-none" required/></div></div></div>
      <div><label className="mb-2 block text-xs font-semibold" htmlFor="evidence">Private evidence <span className="font-normal text-muted-foreground">Optional</span></label><input id="evidence" name="evidence" type="file" accept="image/jpeg,image/png,image/webp" className="block w-full rounded-xl border border-dashed border-input bg-background/60 p-3 text-xs file:mr-3 file:rounded-full file:border-0 file:bg-foreground file:px-3 file:py-1.5 file:text-background"/><p className="mt-2 text-[11px] leading-4 text-muted-foreground">JPG, PNG or WebP up to 5 MB. Hide personal details.</p></div>
      {siteKey&&<><Script src="https://challenges.cloudflare.com/turnstile/v0/api.js" strategy="afterInteractive"/><div className="cf-turnstile" data-sitekey={siteKey} data-theme="light"/></>}
      {!ready&&<p role="status" className="rounded-xl border border-foreground/10 bg-background px-3 py-3 text-xs leading-5 text-muted-foreground">Recording will open after Supabase, private storage, Turnstile, and the administrator allowlist are configured.</p>}{error&&<p role="alert" className="rounded-xl border border-destructive/25 bg-destructive/5 px-3 py-3 text-xs text-destructive">{error}</p>}
      <div className="flex gap-2"><button disabled={saving||!ready} className="h-12 flex-1 rounded-full bg-foreground text-sm font-semibold text-background transition hover:-translate-y-0.5 disabled:translate-y-0 disabled:cursor-not-allowed disabled:opacity-40">{saving?"Recording…":"Submit for review"}</button><button type="button" onClick={()=>{reset();onOpenChange(false)}} className="h-12 rounded-full border border-foreground/12 px-4 text-xs font-semibold">Discard</button></div></form></div>
      <aside className="relative hidden overflow-hidden rounded-r-[27px] bg-foreground p-8 text-background md:block"><div className="absolute -right-20 -top-20 size-64 rounded-full bg-accent/20 blur-3xl"/><p className="relative font-mono text-[10px] uppercase tracking-[.16em] text-background/45">Live receipt</p><div className="relative mt-28"><p className="text-xs text-background/45">Total paid</p><motion.p key={total} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} className="mt-2 text-5xl font-semibold tracking-[-.07em] tabular">{total}</motion.p><div className="mt-7 flex justify-between border-t border-background/12 pt-4 font-mono text-xs text-background/50"><span>{quantity||"0"} account{quantity==="1"?"":"s"}</span><span>{price?money(price):"₱0"} each</span></div></div><div className="absolute inset-x-8 bottom-8"><div className="flex items-center gap-2 text-xs text-accent"><ShieldCheck size={15}/>Evidence stays private</div><p className="mt-3 text-xs leading-5 text-background/45">Approval makes this submission eligible for buyer-reported totals. It does not independently verify the purchase.</p></div></aside>
    </div>}
  </DialogContent></Dialog>;
}
