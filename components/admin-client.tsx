/* eslint-disable @next/next/no-img-element */
"use client";
import { useCallback, useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { money } from "@/lib/domain";

type Factor={id:string;name:string};
type AdminSession={id:string;created_at:string;updated_at:string};
type AccountLink={account_type_slug:string;custom_label:string|null;account_types:{label:string}|null};
type Pending={id:string;seller_id:string;buyer_username:string|null;purchase_date:string;quantity:number;unit_price:string;total_amount:string;loss_issue:string|null;loss_details:string|null;reported_unresolved_amount:string|null;unresolved_amount:string|null;loss_status:string|null;flags:string[];evidence_path:string|null;evidence_sanitized:boolean;linked_duplicate_id:string|null;submitter_fingerprint:string;created_at:string;sellers:{username:string}|null;purchase_account_types:AccountLink[]};
type Loss={id:string;buyer_username:string|null;purchase_date:string;loss_issue:string;loss_details:string;reported_unresolved_amount:string;unresolved_amount:string;loss_status:string;moderation_reason:string|null;created_at:string;sellers:{username:string}|null;purchase_account_types:AccountLink[]};
type Correction={id:string;purchase_id:string;issue_type:string;contact_email:string;details:string;created_at:string};
type Seller={id:string;username:string;status:string;avatar_path:string|null;avatar_updated_at:string|null;created_at:string};
type Audit={id:number;actor:string;action:string;target_type:string;reason?:string|null;created_at:string};
type Blocked={fingerprint:string;blocked_at:string;blocked_by:string};
type SecurityEvent={id:number;event_type:string;outcome:string;request_id:string|null;detail:Record<string,unknown>;created_at:string};
type Queue={pending:Pending[];losses:Loss[];corrections:Correction[];sellers:Seller[];audit:Audit[];blocked:Blocked[];security:SecurityEvent[];accountTypes:Array<{slug:string;label:string}>};
const date=(v:string)=>new Intl.DateTimeFormat("en-PH",{dateStyle:"medium",timeStyle:"short"}).format(new Date(v));
const confirmWords:Record<string,string>={approve:"APPROVE",reject:"REJECT",retract:"RETRACT",adjust_loss:"ADJUST",resolve_loss:"RESOLVE",merge:"MERGE",suspend:"SUSPEND",restore:"RESTORE",resolve:"RESOLVE",dismiss:"DISMISS",block:"BLOCK",unblock:"UNBLOCK",set_account_types:"UPDATE TYPES"};

export function AdminClient({email}:{email:string}){
  const router=useRouter();
  const [data,setData]=useState<Queue|null>(null),[error,setError]=useState(""),[notice,setNotice]=useState(""),[busy,setBusy]=useState(false);
  const [source,setSource]=useState<string|null>(null),[target,setTarget]=useState<string|null>(null),[amounts,setAmounts]=useState<Record<string,string>>({}),[reasons,setReasons]=useState<Record<string,string>>({});
  const [csrf,setCsrf]=useState(""),[factors,setFactors]=useState<Factor[]>([]),[factorId,setFactorId]=useState(""),[totp,setTotp]=useState(""),[unlocked,setUnlocked]=useState(false);
  const [backup,setBackup]=useState<{factorId:string;qrCode:string;secret:string}|null>(null),[backupCode,setBackupCode]=useState("");
  const [sessions,setSessions]=useState<AdminSession[]>([]),[currentSession,setCurrentSession]=useState<string|null>(null);
  const [typeSelections,setTypeSelections]=useState<Record<string,string[]>>({}),[otherTypes,setOtherTypes]=useState<Record<string,string>>({});
  const headers=useMemo(()=>({"content-type":"application/json","x-csrf-token":csrf}),[csrf]);
  const load=useCallback(async()=>{try{const [queueResponse,sessionResponse]=await Promise.all([fetch("/api/admin",{cache:"no-store"}),fetch("/api/auth/session",{cache:"no-store"})]);const queue=await queueResponse.json() as Queue&{error?:string};const session=await sessionResponse.json() as {csrfToken?:string;factors?:Factor[];sessions?:AdminSession[];sessionId?:string|null;error?:string};if(!queueResponse.ok)throw new Error(queue.error);if(!sessionResponse.ok)throw new Error(session.error);setData(queue);setCsrf(session.csrfToken||"");setFactors(session.factors||[]);setSessions(session.sessions||[]);setCurrentSession(session.sessionId||null);setFactorId(current=>current||session.factors?.[0]?.id||"");setError("");}catch(e){setError(e instanceof Error?e.message:"Could not load review queue.");}},[]);
  useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer);},[load]);
  async function unlock(event:React.FormEvent){event.preventDefault();setBusy(true);setError("");try{const response=await fetch("/api/auth/step-up",{method:"POST",headers,body:JSON.stringify({factorId,code:totp})});const result=await response.json() as {error?:string};if(!response.ok)throw new Error(result.error);setTotp("");setUnlocked(true);window.setTimeout(()=>setUnlocked(false),295_000);setNotice("Sensitive actions unlocked for five minutes.");}catch(e){setError(e instanceof Error?e.message:"Authenticator verification failed.");}finally{setBusy(false)}}
  async function act(action:string,body:Record<string,unknown>){const word=confirmWords[action];let confirm="";if(word){confirm=window.prompt(`Type ${word} to confirm.`)||"";if(confirm!==word)return;}setBusy(true);setError("");setNotice("");try{const response=await fetch("/api/admin",{method:"POST",headers,body:JSON.stringify({action,confirm,...body})});const result=await response.json() as {error?:string};if(!response.ok)throw new Error(result.error);setNotice("Action saved.");setSource(null);setTarget(null);await load();}catch(e){setError(e instanceof Error?e.message:"Action failed.");}finally{setBusy(false)}}
  function reportTypes(report:Pending|Loss){return typeSelections[report.id]??report.purchase_account_types.map(value=>value.account_type_slug)}
  function toggleReportType(report:Pending|Loss,slug:string,checked:boolean){setTypeSelections(current=>({...current,[report.id]:checked?[...reportTypes(report),slug]:reportTypes(report).filter(value=>value!==slug)}))}
  async function uploadAvatar(sellerId:string,file:File){setBusy(true);setError("");try{const form=new FormData();form.set("avatar",file);const response=await fetch(`/api/admin/sellers/${sellerId}/avatar`,{method:"POST",headers:{"x-csrf-token":csrf},body:form});const result=await response.json() as {error?:string};if(!response.ok)throw new Error(result.error);setNotice("Seller image updated.");await load();}catch(e){setError(e instanceof Error?e.message:"Image upload failed.");}finally{setBusy(false)}}
  async function removeAvatar(sellerId:string){if(window.prompt("Type REMOVE IMAGE to confirm.")!=="REMOVE IMAGE")return;setBusy(true);setError("");try{const response=await fetch(`/api/admin/sellers/${sellerId}/avatar`,{method:"DELETE",headers,body:JSON.stringify({confirm:"REMOVE IMAGE",reason:"Moderator removed seller reference image"})});const result=await response.json() as {error?:string};if(!response.ok)throw new Error(result.error);setNotice("Seller image removed.");await load();}catch(e){setError(e instanceof Error?e.message:"Image removal failed.");}finally{setBusy(false)}}
  async function signOut(){setBusy(true);try{await fetch("/api/auth/signout",{method:"POST",headers});router.push("/admin");router.refresh();}finally{setBusy(false)}}
  async function addBackupFactor(){setBusy(true);setError("");try{const response=await fetch("/api/auth/mfa/add",{method:"POST",headers});const result=await response.json() as {factorId?:string;qrCode?:string;secret?:string;error?:string};if(!response.ok||!result.factorId||!result.qrCode||!result.secret)throw new Error(result.error||"Could not add a backup factor.");setBackup({factorId:result.factorId,qrCode:result.qrCode,secret:result.secret});}catch(e){setError(e instanceof Error?e.message:"Could not add a backup factor.");}finally{setBusy(false)}}
  async function verifyBackup(event:React.FormEvent){event.preventDefault();if(!backup)return;setBusy(true);setError("");try{const response=await fetch("/api/auth/mfa/verify",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({factorId:backup.factorId,code:backupCode})});const result=await response.json() as {error?:string};if(!response.ok)throw new Error(result.error);setBackup(null);setBackupCode("");setNotice("Backup authenticator added.");await load();}catch(e){setError(e instanceof Error?e.message:"Backup factor verification failed.");}finally{setBusy(false)}}
  const button="min-h-10 border border-foreground/20 bg-card px-3.5 py-2 text-xs font-semibold transition hover:bg-foreground hover:text-background disabled:opacity-50";
  return <main className="mx-auto max-w-[1240px] space-y-14 px-5 py-16 md:px-8 md:py-24">
    <header className="grid gap-6 border-b border-foreground/12 pb-10 md:grid-cols-[1fr_auto] md:items-end"><div><p className="font-mono text-[10px] uppercase tracking-[.16em] text-muted-foreground">Private workspace</p><h1 className="mt-3 text-5xl font-semibold tracking-[-.07em]">Moderation</h1><p className="mt-3 text-sm text-muted-foreground">Signed in as {email}. Approval makes a report eligible for aggregation; it does not independently verify it.</p></div><div className="flex items-center gap-3">{data&&<div className="border border-foreground px-4 py-2 font-mono text-xs">{data.pending.length} pending</div>}<button disabled={busy||!csrf} onClick={signOut} className={button}>Sign out everywhere</button></div></header>
    <section className="border border-foreground/20 bg-card p-5"><div className="grid gap-5 md:grid-cols-[1fr_auto] md:items-end"><div><h2 className="text-xl font-semibold">Sensitive-action lock</h2><p className="mt-2 text-sm text-muted-foreground">Evidence and destructive actions require a fresh authenticator code. {unlocked?"Unlocked for this five-minute window.":"Currently locked."}</p><p className="mt-2 text-xs text-muted-foreground">Enrolled factors: {factors.length}. Keep two factors on separate devices.</p></div><form onSubmit={unlock} className="flex flex-wrap gap-2">{factors.length>1&&<select aria-label="Authenticator factor" value={factorId} onChange={e=>setFactorId(e.target.value)} className="h-11 border border-foreground/20 bg-background px-2 text-sm">{factors.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select>}<input aria-label="Six-digit authenticator code" value={totp} onChange={e=>setTotp(e.target.value.replace(/\D/g,"").slice(0,6))} inputMode="numeric" autoComplete="one-time-code" placeholder="000000" className="h-11 w-28 border border-foreground/20 bg-background px-3 font-mono"/><button disabled={busy||totp.length!==6||!csrf} className={button}>Unlock</button></form></div><div className="mt-5 border-t border-foreground/20 pt-5"><h3 className="text-sm font-semibold">Active sessions</h3><div className="mt-2 space-y-1 text-xs text-muted-foreground">{sessions.map(session=><p key={session.id}><span className="font-mono">{session.id===currentSession?"Current · ":""}{session.id.slice(0,8)}…</span> · active {date(session.updated_at)}</p>)}</div></div>{factors.length<2&&!backup&&<button type="button" disabled={busy||!unlocked} onClick={addBackupFactor} className={`${button} mt-5`}>Add backup authenticator</button>}{backup&&<form onSubmit={verifyBackup} className="mt-5 grid gap-4 border-t border-foreground/20 pt-5 sm:grid-cols-[auto_1fr]"><img src={backup.qrCode} alt="Backup authenticator enrollment QR code" className="size-44"/><div><p className="text-sm font-semibold">Scan with a second device or authenticator.</p><p className="mt-2 break-all font-mono text-[11px]">Manual key: {backup.secret}</p><div className="mt-4 flex gap-2"><input aria-label="Backup authenticator code" value={backupCode} onChange={e=>setBackupCode(e.target.value.replace(/\D/g,"").slice(0,6))} inputMode="numeric" placeholder="000000" className="h-11 w-28 border border-foreground/20 bg-background px-3 font-mono"/><button disabled={busy||backupCode.length!==6} className={button}>Verify backup</button></div></div></form>}</section>
    {error&&<p role="alert" className="border border-foreground bg-card p-3 text-sm">{error}</p>}{notice&&<p role="status" className="border border-foreground bg-card p-3 text-sm">{notice}</p>}
    {!data?<p>Loading review queue…</p>:<>
      <section><h2 className="mb-4 text-xl font-semibold">Pending reports <span className="text-muted-foreground">{data.pending.length}</span></h2><div className="space-y-3">{data.pending.length===0?<Empty/>:data.pending.map(p=><article key={p.id} className="border border-border bg-card p-5"><div className="grid gap-5 lg:grid-cols-[1fr_auto]"><div><p className="font-semibold">@{p.sellers?.username||"Unknown seller"} · {p.unresolved_amount?`${money(p.unresolved_amount)} unresolved`:money(p.total_amount)}</p><p className="mt-1 text-xs text-muted-foreground">Buyer: {p.buyer_username||"Not recorded"} · {p.quantity} × {money(p.unit_price)} · purchase {p.purchase_date} · submitted {date(p.created_at)}</p>{p.loss_issue&&<p className="mt-3 text-xs font-semibold uppercase tracking-wide">{p.loss_issue.replaceAll("_"," ")}</p>}{p.loss_details&&<p className="mt-2 max-w-2xl text-sm">{p.loss_details}</p>}<AccountTypeEditor report={p} catalog={data.accountTypes} selected={reportTypes(p)} other={otherTypes[p.id]??p.purchase_account_types.find(value=>value.account_type_slug==="other")?.custom_label??""} onToggle={(slug,checked)=>toggleReportType(p,slug,checked)} onOther={value=>setOtherTypes(current=>({...current,[p.id]:value}))} onSave={()=>act("set_account_types",{id:p.id,accountTypes:reportTypes(p),otherAccountType:otherTypes[p.id]??p.purchase_account_types.find(value=>value.account_type_slug==="other")?.custom_label??"",reason:"Moderator corrected account types"})} disabled={busy||!unlocked}/><p className="mt-2 text-xs font-medium">Evidence: {p.evidence_path?(p.evidence_sanitized?"sanitized private image":"legacy private image"):"none"}</p>{p.flags.length>0&&<p className="mt-2 text-xs font-medium">Risk flags: {p.flags.join(", ")}</p>}{p.linked_duplicate_id&&<p className="mt-2 text-xs">Linked duplicate: {p.linked_duplicate_id}</p>}<p className="mt-2 text-xs text-muted-foreground">Record {p.id}</p>{!p.evidence_path&&<div className="mt-4"><label htmlFor={`reason-${p.id}`} className="mb-1 block text-xs font-semibold">Required rationale to approve without evidence</label><textarea id={`reason-${p.id}`} value={reasons[p.id]||""} onChange={e=>setReasons(r=>({...r,[p.id]:e.target.value}))} maxLength={500} className="w-full border border-foreground/20 bg-background p-2 text-sm"/></div>}</div><div className="flex flex-wrap content-start gap-2">{p.evidence_path&&(unlocked?<a href={`/api/admin/evidence/${p.id}`} className={button}>Download evidence</a>:<button className={button} onClick={()=>setError("Unlock sensitive actions before downloading evidence.")}>Evidence locked</button>)}<button disabled={busy||!unlocked||(!p.evidence_path&&(reasons[p.id]||"").trim().length<10)} className={button} onClick={()=>act("approve",{id:p.id,reason:reasons[p.id]||"Evidence reviewed"})}>Approve</button><button disabled={busy||!unlocked} className={button} onClick={()=>act("reject",{id:p.id,reason:reasons[p.id]||"Rejected during moderation"})}>Reject</button><button disabled={busy||!unlocked} className={button} onClick={()=>act("block",{fingerprint:p.submitter_fingerprint,reason:"Abusive submission pattern"})}>Block submitter</button></div></div></article>)}</div></section>
      <section><h2 className="mb-4 text-xl font-semibold">Approved loss reports <span className="text-muted-foreground">{data.losses.length}</span></h2><div className="space-y-3">{data.losses.length===0?<Empty/>:data.losses.map(p=><article key={p.id} className="border border-border bg-card p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="font-semibold">@{p.sellers?.username||"Unknown seller"} · {money(p.unresolved_amount)} unresolved</p><p className="mt-1 text-xs text-muted-foreground">Buyer: {p.buyer_username||"Not recorded"} · originally {money(p.reported_unresolved_amount)} · {p.loss_status} · {p.purchase_date}</p><p className="mt-3 text-sm">{p.loss_issue.replaceAll("_"," ")}: {p.loss_details}</p>{p.moderation_reason&&<p className="mt-2 text-xs text-muted-foreground">Review rationale: {p.moderation_reason}</p>}<AccountTypeEditor report={p} catalog={data.accountTypes} selected={reportTypes(p)} other={otherTypes[p.id]??p.purchase_account_types.find(value=>value.account_type_slug==="other")?.custom_label??""} onToggle={(slug,checked)=>toggleReportType(p,slug,checked)} onOther={value=>setOtherTypes(current=>({...current,[p.id]:value}))} onSave={()=>act("set_account_types",{id:p.id,accountTypes:reportTypes(p),otherAccountType:otherTypes[p.id]??p.purchase_account_types.find(value=>value.account_type_slug==="other")?.custom_label??"",reason:"Moderator corrected account types"})} disabled={busy||!unlocked}/></div><div className="flex flex-wrap items-center gap-2"><input aria-label={`Adjusted unresolved amount for @${p.sellers?.username}`} inputMode="decimal" value={amounts[p.id]??p.unresolved_amount} onChange={e=>setAmounts(a=>({...a,[p.id]:e.target.value}))} className="h-10 w-28 border border-foreground/20 bg-background px-2 text-sm"/><button disabled={busy||!unlocked} className={button} onClick={()=>act("adjust_loss",{id:p.id,amount:amounts[p.id]??p.unresolved_amount,reason:"Refund or correction adjustment"})}>Save amount</button><button disabled={busy||!unlocked||p.loss_status==="resolved"} className={button} onClick={()=>act("resolve_loss",{id:p.id,reason:"Loss resolved"})}>Resolve</button><button disabled={busy||!unlocked} className={button} onClick={()=>act("retract",{id:p.id,reason:"Report retracted"})}>Retract</button></div></div></article>)}</div></section>
      <section><h2 className="mb-4 text-xl font-semibold">Correction requests <span className="text-muted-foreground">{data.corrections.length}</span></h2><div className="space-y-3">{data.corrections.length===0?<Empty/>:data.corrections.map(c=><article key={c.id} className="border border-border bg-card p-5"><p className="font-semibold">{c.issue_type.replaceAll("_"," ")} · {c.purchase_id}</p><p className="mt-1 text-sm text-muted-foreground">{c.contact_email} · {date(c.created_at)}</p>{c.details&&<p className="mt-2 text-sm">{c.details}</p>}<div className="mt-4 flex flex-wrap gap-2"><button disabled={busy||!unlocked} className={button} onClick={()=>act("retract",{id:c.purchase_id,reason:"Correction request"})}>Retract record</button><button disabled={busy||!unlocked} className={button} onClick={()=>act("resolve",{id:c.id,reason:"Correction handled"})}>Mark resolved</button><button disabled={busy||!unlocked} className={button} onClick={()=>act("dismiss",{id:c.id,reason:"Correction dismissed"})}>Dismiss</button></div></article>)}</div></section>
      <section><h2 className="mb-4 text-xl font-semibold">Sellers</h2><div className="mb-4 flex flex-wrap items-center gap-3 border border-border bg-card p-4 text-sm"><span>Source: {data.sellers.find(s=>s.id===source)?.username||"None"}</span><span>→</span><span>Target: {data.sellers.find(s=>s.id===target)?.username||"None"}</span><button disabled={busy||!unlocked||!source||!target||source===target} onClick={()=>source&&target&&act("merge",{source,target,reason:"Duplicate seller handles"})} className={button}>Merge sellers</button></div><div className="space-y-2">{data.sellers.map(s=><div key={s.id} className="flex flex-wrap items-center justify-between gap-3 border border-border bg-card p-3 text-sm"><span className="flex items-center gap-3">{s.avatar_path?<img src={`/api/sellers/${s.id}/avatar?v=${encodeURIComponent(s.avatar_updated_at||"")}`} alt="" className="size-10 border border-foreground/20 object-cover"/>:<span className="grid size-10 place-items-center border border-foreground/20 font-mono uppercase">{s.username.slice(0,1)}</span>}@{s.username} · {s.status}</span><div className="flex flex-wrap gap-2"><label className={`${button} inline-flex items-center`}>Upload image<input type="file" accept="image/jpeg,image/png,image/webp" className="sr-only" disabled={busy||!unlocked||s.status==="merged"} onChange={e=>{const file=e.target.files?.[0];if(file)void uploadAvatar(s.id,file);e.currentTarget.value=""}}/></label>{s.avatar_path&&<button disabled={busy||!unlocked} className={button} onClick={()=>void removeAvatar(s.id)}>Remove image</button>}<button className={button} onClick={()=>setSource(s.id)}>Source</button><button className={button} onClick={()=>setTarget(s.id)}>Target</button><button disabled={busy||!unlocked||s.status==="merged"} className={button} onClick={()=>act(s.status==="suspended"?"restore":"suspend",{id:s.id,reason:"Moderator seller status change"})}>{s.status==="suspended"?"Restore":"Suspend"}</button></div></div>)}</div></section>
      <section><h2 className="mb-4 text-xl font-semibold">Blocked submitters</h2><div className="space-y-2">{data.blocked.length===0?<Empty/>:data.blocked.map(b=><div key={b.fingerprint} className="flex flex-wrap items-center justify-between gap-3 border border-border bg-card p-3 text-sm"><span className="font-mono">{b.fingerprint.slice(0,12)}… · {date(b.blocked_at)}</span><button disabled={busy||!unlocked} className={button} onClick={()=>act("unblock",{fingerprint:b.fingerprint,reason:"Moderator unblock"})}>Unblock</button></div>)}</div></section>
      <section><h2 className="mb-4 text-xl font-semibold">Security events</h2><div className="space-y-2">{data.security.length===0?<Empty/>:data.security.map(event=><div key={event.id} className="border border-border bg-card p-3 text-sm"><span className="font-semibold">{event.event_type.replaceAll("_"," ")}</span> · {event.outcome} · {date(event.created_at)}{event.request_id&&<span className="block font-mono text-[11px] text-muted-foreground">{event.request_id}</span>}</div>)}</div></section>
      <section><h2 className="mb-4 text-xl font-semibold">Audit history</h2><div className="space-y-2">{data.audit.length===0?<Empty/>:data.audit.map(a=><div key={a.id} className="border border-border bg-card p-3 text-sm"><span className="font-semibold">{a.action.replaceAll("_"," ")}</span> · {a.actor} · {date(a.created_at)}{a.reason&&<span className="block text-xs text-muted-foreground">{a.reason}</span>}</div>)}</div></section>
    </>}
  </main>;
}
function Empty(){return <div className="border border-dashed border-foreground/20 bg-card p-5 text-sm text-muted-foreground">Nothing to review.</div>}
function AccountTypeEditor({catalog,selected,other,onToggle,onOther,onSave,disabled}:{report:Pending|Loss;catalog:Array<{slug:string;label:string}>;selected:string[];other:string;onToggle:(slug:string,checked:boolean)=>void;onOther:(value:string)=>void;onSave:()=>void;disabled:boolean}){
  return <div className="mt-4 border border-foreground/15 p-3"><p className="text-xs font-semibold">Account types</p><div className="mt-2 flex flex-wrap gap-2">{catalog.map(type=><label key={type.slug} className="flex items-center gap-1.5 border border-foreground/20 px-2 py-1 text-[11px]"><input type="checkbox" checked={selected.includes(type.slug)} onChange={event=>onToggle(type.slug,event.target.checked)}/>{type.label}</label>)}</div>{selected.includes("other")&&<input value={other} onChange={event=>onOther(event.target.value)} maxLength={40} placeholder="Custom account type" className="mt-2 h-9 border border-foreground/20 bg-background px-2 text-xs"/>}<button type="button" disabled={disabled||selected.length<1||selected.length>8} onClick={onSave} className="mt-2 min-h-9 border border-foreground/20 px-3 text-xs font-semibold disabled:opacity-50">Save account types</button></div>;
}
