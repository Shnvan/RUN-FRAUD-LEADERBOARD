import { notFound } from "next/navigation";
import { SiteShell } from "@/components/site-shell";
import { CorrectionForm } from "@/components/correction-form";
import { dbGet } from "@/lib/server";
import { money, type PublicPurchase } from "@/lib/domain";
export const dynamic="force-dynamic";
export default async function CorrectPage({params}:{params:Promise<{id:string}>}){const id=(await params).id;if(!/^[0-9a-f-]{36}$/i.test(id))notFound();const records=await dbGet<PublicPurchase[]>("public_purchase_rows",`select=*&id=eq.${id}&limit=1`);const record=records[0];if(!record)notFound();return <SiteShell><main className="mx-auto max-w-[1240px] px-5 py-16 md:px-8 md:py-24"><div className="grid gap-10 lg:grid-cols-12"><div className="lg:col-span-4"><p className="font-mono text-[10px] uppercase tracking-[.16em] text-muted-foreground">Private request</p><h1 className="mt-4 text-5xl font-semibold leading-[.95] tracking-[-.07em]">Report an issue with this record.</h1><p className="mt-5 text-sm leading-6 text-muted-foreground">Your correction, contact details, and explanation are visible only to moderators.</p><div className="mt-8 rounded-[22px] bg-foreground p-5 text-background"><p className="font-semibold">@{record.username}</p><p className="mt-8 font-mono text-2xl font-semibold tabular">{money(record.total_amount)}</p><p className="mt-2 text-xs text-background/50">{record.quantity} × {money(record.unit_price)} · {record.purchase_date}</p></div></div><div className="lg:col-span-7 lg:col-start-6"><CorrectionForm purchaseId={id} siteKey={process.env.TURNSTILE_SITE_KEY||""}/></div></div></main></SiteShell>}
